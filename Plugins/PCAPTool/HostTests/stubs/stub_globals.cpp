#include "CoreMinimal.h"

const FVector   FVector::ZeroVector   = FVector(0.f, 0.f, 0.f);
const FRotator  FRotator::ZeroRotator = FRotator(0.f, 0.f, 0.f);
const FQuat     FQuat::Identity       = FQuat(0.f, 0.f, 0.f, 1.f);
